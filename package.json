{
  "scripts": {
    "build": "sh -c 'cd packages/fontkit && npm run trie:data && npm run trie:use && npm run trie:indic' && turbo run build --filter=jspdf-fontkit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "turbo": "1.13.3",
    "prettier": "3.2.5"
  }
}
